import { MaintenancePredictor } from '@/components/maintenance/MaintenancePredictor';
export const metadata = { title: "Mechanic's Eye — LogiMargin" };
export default function MaintenancePage() { return <MaintenancePredictor />; }
